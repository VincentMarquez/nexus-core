# NEXUS durable operator plugin

This bundled example groups the public operator surfaces for inspecting and
resuming crash-safe NEXUS tasks.

- Agent: `durable-operator`
- Skill: `durable-operator-board`
- Command: `task-board`

The plugin is classified as `ops`. Review task evidence and approval state
before resuming or cancelling work.
