# Meta-review (alive cycle)

ts: 2026-07-17T06:03:23Z
goal: Maximize official SWE-bench Pro resolve rate with multi-AI group review: Claude plan+review, Grok implement, Codex adversary, Gemini arXiv/web, local files. Score only via official Pro Docker harness. Aspiration toward highest SWE coding; 100% Pro not currently realistic for any public stack.
verdict: **implemented_green**
tests_ok: True
phases_seen: github_review, record_preferences, github_ge_5k_input, arxiv_review, paper_improve, dual_review, canonical_engine, idea_portfolio, self_check, fix_loop, self_approve_decision, implement, self_check, fix_loop, promote_on_done, improvements_log, plan_snapshot, arxiv_snapshot, self_check_after_apply, self_check_meta

## Review phases

- github_review: ok
- arxiv_review: ok
- implement: {'step': 'implement', 'ok': True, 'mode': 'idea_portfolio', 'implemented': 10, 'total': 10, 'arxiv_done': 2, 'github_done': 2, 'cross_done': 6, 'results': [{'id': 'arxiv:2606.26649v1', 'source': 'arxiv', 'selected_as': 'required_arxiv', 'worker': 'grok', 'ok': True, 'result': {'ok': True, 'text': "I'll implement a Cedar Policy Language validation step in the consensus path before promotion. First I'll read the self-improve docs and locate the consensus module.Next I'll inspect the consensus module and any existing Cedar/policy-as-code patterns to wire validation before promotion.I'll inspect how consensus feeds promotion and which security-gate patterns already exist so we can add a Cedar-style validation step cleanly.Implementing a Cedar-shaped policy validator and wiring it into consensus before promote.Fixing a couple of parser edge cases, then wiring the promote gate into consensus and the engine.Checking promote tests so the Cedar gate doesn't break the single-judge path.Adjusting the engine promote path so single-judge verdicts aren't blocked by the multi-grader Cedar defaults.The preview truncates before any `permit` lines — adjusting the test and the preview content.All targeted tests passed. Updating docs and running the full suite.## Landed: Cedar policy-as-code before consensus promote\n\n**Source:** [arXiv 2606.26649v1](https://arxiv.org/abs/2606.26649v1) — *Autoformalization of Agent Instructions into Policy-as-Code*  \n**Concrete change:** Cedar-shaped validation in the consensus module before promoting a decision.\n\n### What changed\n\n| File | Role |\n|------|------|\n| `src/nexus/cedar_policy.py` | New offline Cedar subset (`nexus.cedar_policy/v1`): permit/forbid, forbid > permit > deny_default, when-atoms, text parse/dump |\n| `src/nexus/consensus.py` | `validate_promote`, `promote_decision`, `ConsensusVerdict.apply_promote_gate`; every aggregate attaches `promote_allowed` + `cedar_policy` |\n| `src/nexus/engine.py` | Review→promote path runs Cedar after IndependentVerify (opt-out: `meta.cedar_policy=false`) |\n| `tests/test_cedar_policy.py` | Policy eval, parse, promote gate, consensus integration |\n| `tests/test_consensus.py` | Aggregate + engine promote Cedar coverage |\n| `docs/LATEST_IMPROVE_PLAN.md`, `docs/SELF_IMPROVE_CYCLE.md`, `docs/ALIVE_IMPROVEMENTS.md` | Cycle docs |\n\n### Behavior\n\nDefault promote policies (fail-closed):\n\n- **Forbid:** `fail` / veto-family, `degraded`, low score, low agreement  \n- **Permit:** healthy `pass` (and high-score `revise`) with enough graders  \n- Engine still uses IndependentVerify; Cedar is an extra formal policy layer  \n- Align floors with `meta.verify_min_score` / `meta.cedar_min_score` when set  \n\n### Tests\n\n```text\nPYTHONPATH=src python3 -m pytest -q\n→ 699 passed, 1 skipped\n```\n\nNo vendored Cedar engine, no force-push, no secrets.", 'error': None, 'model': 'grok-4.5', 'exit_code': None}}, {'id': 'wshobson/agents', 'source': 'github', 'selected_as': 'required_github', 'worker': 'grok', 'ok': True, 'result': {'ok': True, 'text': "I'll start by reading the self-improvement docs and scanning the repo for the first-apply slice from the plan.Marketplace already landed from wshobson; I'll inspect it and the scout clone for the next high-value slice.Marketplace is substantial already — I'll compare it with wshobson adapters/validation for a still-open, high-value gap.Marketplace core is solid; I'll port wshobson's round-trip integrity check plus Makefile quality gates as a small tested slice.Implementing round-trip integrity (source→generate→count→validate), CLI/MCP wiring, and a Makefile quality gate.Adding round-trip integrity APIs and wiring them through CLI, MCP, Makefile, and tests.Fixing count logic so expected counts work for synthetic plugins (list fields when filesystem is empty):## Landed: wshobson/agents round-trip integrity gate\n\nPorted the **round-trip quality gate** pattern from [wshobson/agents](https://github.com/wshobson/agents) (`tools/tests/test_round_trip.py` + Makefile validate flow) into the existing marketplace — no upstream tree vendored.\n\n### What changed\n\n| Area | Change |\n|------|--------|\n| `src/nexus/marketplace.py` | `expected_counts_for_harness`, `check_round_trip_counts`, `round_trip`, `smoke_round_trip` — generate → source/expected counts → `validate_generated` |\n| `src/nexus/cli.py` | `nexus marketplace round-trip` |\n| `src/nexus/mcp_server.py` | MCP action `round_trip` |\n| `Makefile` | `marketplace-check` + `marketplace-round-trip` in `test-quality` |\n| `tests/test_marketplace.py` | 6 new cases (map counts, clean trip, sabotage mismatch, CLI, MCP, seed plugin) |\n| Docs | `LATEST_IMPROVE_PLAN.md`, `SELF_IMPROVE_CYCLE.md`, `ALIVE_IMPROVEMENTS.md`, `plugins/README.md` |\n\n### Behavior\n\nCodex/Copilot map commands → skills; the gate checks post-adapter counts match capability-aware expectations so silent skips/dupes fail CI.\n\n### Evidence\n\n```text\nPYTHONPATH=src python3 -m pytest -q  →  705 passed, 1 skipped\nsmoke_round_trip('.')                →  ok=True (claude/codex/copilot × nexus-durable)\n```\n\n### Non-goals kept\n\n- No vendored 94-plugin tree  \n- No global `~/.config` install symlinks  \n- No force-push / secrets  \n\n### Prove locally\n\n```bash\nPYTHONPATH=src python3 -m pytest -q tests/test_marketplace.py\nmake marketplace-check marketplace-round-trip\n```", 'error': None, 'model': 'grok-4.5', 'exit_code': None}}, {'id': 'arxiv:2512.11213v2', 'source': 'arxiv', 'selected_as': 'diversity_arxiv', 'worker': 'grok', 'ok': True, 'result': {'ok': True, 'text': 'I\'ll implement a budget-aware resource allocation module from the FutureWeaver idea. First I\'ll read the self-improve docs and inspect the orchestrator so we can scope a small, tested change.There\'s already budget plumbing from prior cycles. Next I\'ll inspect the orchestrator, existing budget modules, and any FutureWeaver notes so we can land a small modular allocation slice.There\'s already an orchestrator and budget primitives — I\'ll read those and plan a multi-agent allocation module that fits them.I\'ll design a multi-agent budget allocator that builds on `RunBudget` and wire it into the orchestrator.Implementing a FutureWeaver-style multi-agent compute pool and wiring it into the orchestrator.Implementing the allocator module and integrating it with the orchestrator.Wiring the allocator into the orchestrator next.Running the new tests, then updating the docs.All 21 budget tests passed. Updating docs and running the full suite.## Landed: FutureWeaver multi-agent compute budget\n\n**Source:** [arxiv:2512.11213v2](https://arxiv.org/abs/2512.11213v2) — *FutureWeaver: Planning Test-Time Compute for Multi-Agent Systems with Modularized Collaboration*\n\n**Concrete change:** Budget-aware resource allocation in the orchestrator that tracks and limits compute usage across agents.\n\n### What was added\n\n| Piece | Role |\n|--------|------|\n| `src/nexus/budget_alloc.py` | Shared test-time compute pool (`nexus.budget_alloc/v1`) |\n| Strategies | `equal` · `weighted` (role weights; implementer heaviest) · `modular` (reserved floor + reclaim) |\n| Lifecycle | `plan` → `grant`/`consume` → `finish`/`reclaim` → `rebalance` residual |\n| Orchestrator wire-up | Plan on `meta.compute_budget`; status + `record_agent_usage` hard-stop |\n| Engine handoff | Worker injects `budget_alloc` + mirrors `total_tokens` → `max_tokens` |\n\nComposes with existing `RunBudget` (per-run) and `usage.Budget` (daily/monthly); does not replace them.\n\n### Usage sketch\n\n```python\norch.run_task("…", agent_mode="fake", meta={\n    "compute_budget": {\n        "total_tokens": 1000,\n        "strategy": "weighted",  # or equal | modular\n        "agents": ["planner", "implementer", "tester", "reviewer"],\n    },\n}, sync_fake=True)\n\norch.record_agent_usage(task_id, "implementer", tokens=200)\norch.record_agent_usage(task_id, "planner", tokens=50, finish=True, rebalance=True)\n```\n\n### Files changed (this slice)\n\n- **New:** `src/nexus/budget_alloc.py`, `tests/test_budget_alloc.py`\n- **Edited:** `src/nexus/orchestrator.py`\n- **Docs:** `docs/LATEST_IMPROVE_PLAN.md`, `docs/ALIVE_IMPROVEMENTS.md`, `docs/SELF_IMPROVE_CYCLE.md`\n\n### Tests\n\n```\nPYTHONPATH=src python3 -m pytest -q\n→ 726 passed, 1 skipped\n```\n\n(`tests/test_budget_alloc.py`: 21 passed — equal/weighted/modular, hard exhaust, reclaim/rebalance, orchestrator integration)\n\n### Next open (deferred)\n\n- MCP tool `compute_budget`\n- Engine auto-record per step agent\n- CLI `nexus task budget`', 'error': None, 'model': 'grok-4.5', 'exit_code': None}}, {'id': 'novel:arxiv:2512.11213v2+builderz-labs/mission-control', 'source': 'cross_pattern', 'selected_as': 'cross_pattern', 'worker': 'grok', 'ok': True, 'result': {'ok': True, 'text': "Docs already list a landed budget allocator; next I'll inspect that code and mission-control's control-plane/spend patterns for a hybrid slice.Budget allocator is already in-tree; I'll inspect mission-control spend/governance and our open next-slice hooks for a hybrid control-plane upgrade.The pure FutureWeaver slice is already landed; next I'll hybridize it with mission-control's SQLite spend/governance patterns.I'll implement a small hybrid that binds FutureWeaver allocation to the SQLite ops control plane, then wire thin CLI/MCP surfaces.Implementing `budget_plane.py` (FutureWeaver × mission-control SQLite) plus CLI/MCP surfaces and tests.Wiring orchestrator, CLI, MCP, and tool catalog next.## Landed: FutureWeaver × mission-control budget plane\n\nCross-pattern hybrid for portfolio **novel:arxiv:2512.11213v2+builderz-labs/mission-control**. Pure FutureWeaver (`budget_alloc`) was already in-tree; this session binds it to the SQLite ops control plane (mission-control shape).\n\n### What changed\n\n| File | Change |\n|------|--------|\n| `src/nexus/budget_plane.py` | **New** — `BudgetPlane` plan/bind/record/finish/rebalance, agent spend board, `dispatch` |\n| `src/nexus/orchestrator.py` | Full `budget_alloc` on ops job; plane bind + spend sync on usage |\n| `src/nexus/cli.py` | `nexus ops budget plan\\|status\\|record\\|report\\|brief\\|rebalance\\|finish` |\n| `src/nexus/mcp_server.py` | MCP tool **`compute_budget`** |\n| `src/nexus/tool_catalog.py` | Privilege map for `compute_budget` |\n| `tests/test_budget_plane.py` | **New** — unit + orch + CLI + MCP |\n| `docs/LATEST_IMPROVE_PLAN.md` | Plan rewritten for this slice |\n| `docs/SELF_IMPROVE_CYCLE.md` | Cycle note |\n| `docs/ALIVE_IMPROVEMENTS.md` | Appended cycle log |\n\n### Behavior\n\n1. **Plan** multi-agent compute (equal / weighted / modular) via FutureWeaver allocator  \n2. **Bind** full quota snapshot to SQLite ops job `meta.budget_alloc`  \n3. **Record** per-agent usage with hard limits; attribute spend as `agent:<name>`  \n4. **Finish / rebalance** modular residual shares  \n5. **Report** agent spend board for the operator surface  \n\n### Prove\n\n```bash\nPYTHONPATH=src python3 -m pytest -q tests/test_budget_plane.py\n# 9 passed\n\nPYTHONPATH=src python3 -m pytest -q\n# 735 passed, 1 skipped\n```\n\n### Example\n\n```bash\nnexus ops budget plan --job-id j1 --total-tokens 1000 --strategy modular --agents planner,implementer\nnexus ops budget record --job-id j1 --agent implementer --tokens 200\nnexus ops budget status --job-id j1\n```\n\n**Non-goals kept:** no vendored FutureWeaver/mission-control trees; no force-push; composes with existing `budget_alloc` / `ops_store` / `RunBudget`.", 'error': None, 'model': 'grok-4.5', 'exit_code': None}}, {'id': 'novel:arxiv:2407.01476v4+wshobson/agents', 'source': 'cross_pattern', 'selected_as': 'cross_pattern', 'worker': 'grok', 'ok': True, 'result': {'ok': True, 'text': "I'll start by reading the self-improve docs and the improve plan so we can land a small, tested slice of the cross-pattern planner idea.The current docs describe a different hybrid; next I'll inspect existing planner/control-plane modules and the portfolio entry for arXiv 2407.01476.There's already a marketplace planner and control-plane planner; I'll inspect those plus the paper idea so we can add a small search-based hybrid cleanly.Implementing a focused search-based planner module (beam + A*) over the marketplace catalog to guide the control plane, with tests and docs.Next I'll add tests and wire the CLI.All unit tests passed. Updating the improve docs next, then running the full suite.## Landed: Tree Search × marketplace plane guide\n\nPortfolio **[cross_pattern] novel:arxiv:2407.01476v4+wshobson/agents** — beam/A* search over the Markdown marketplace catalog to guide the control plane and Orchestrator handoff.\n\n### What shipped\n\n| Piece | Role |\n|--------|------|\n| `src/nexus/search_plane_planner.py` | Beam + A* over hybrid marketplace + `plane.*` catalog |\n| `tests/test_search_plane_planner.py` | 21 tests (catalog, search, pure plan, guide, handoff, CLI) |\n| `src/nexus/cli.py` | `nexus tool-agent search-{catalog,plan,guide,handoff,prompt}` |\n| Docs | `LATEST_IMPROVE_PLAN.md`, `SELF_IMPROVE_CYCLE.md`, `ALIVE_IMPROVEMENTS.md` |\n\n### Design (small hybrid)\n\n1. **Action space** — wshobson-shaped marketplace agents/skills/commands (+ optional `plane.*` ops).\n2. **Tree search** (arXiv 2407.01476) — `beam_search` / `astar_search` with `g = step_cost(score)`, `h = uncovered task-token fraction`.\n3. **Control-plane guide** — plan shell: `upsert(inbox) → running → [search components] → spend → completed`.\n4. **`plan_and_guide`** — runs only `plane.*` steps; stamps full search plan into ops job meta (marketplace steps planned, not auto-executed).\n5. **`plan_and_handoff`** — search → guide → `Orchestrator.run_task(with_plan=True)`.\n\n### Prove\n\n```bash\nPYTHONPATH=src python3 -m pytest -q tests/test_search_plane_planner.py  # 21 passed\nPYTHONPATH=src python3 -m pytest -q                                   # 756 passed, 1 skipped\n```\n\n### Files touched this session\n\n- **New:** `src/nexus/search_plane_planner.py`, `tests/test_search_plane_planner.py`\n- **Wired:** `src/nexus/cli.py` (search-* tool-agent commands)\n- **Docs:** `docs/LATEST_IMPROVE_PLAN.md`, `docs/SELF_IMPROVE_CYCLE.md`, `docs/ALIVE_IMPROVEMENTS.md`\n\n### Non-goals kept\n\nNo vendored paper/upstream trees, no live LLM scorer, greedy `marketplace_planner` / `control_plane_planner` left orthogonal, no force-push.", 'error': None, 'model': 'grok-4.5', 'exit_code': None}}, {'id': 'novel:arxiv:2602.22953v2+wshobson/agents', 'source': 'cross_pattern', 'selected_as': 'cross_pattern', 'worker': 'grok', 'ok': True, 'result': {'ok': True, 'text': "I'll start by reading the self-improvement docs and the plan, then implement the first apply slice as a small, tested change.The new goal differs from the landed search-plane planner: a unifying protocol layer over tools/CLI, shaped by the marketplace pattern. Exploring existing modules and the wshobson structure.Implementing a small `agent_protocol` module that unifies tool/CLI/marketplace surfaces into one envelope, with tests.All 20 protocol tests passed. Updating docs and running the full suite.## Landed: unifying agent protocol layer\n\n**Portfolio idea:** `[cross_pattern] novel:arxiv:2602.22953v2+wshobson/agents`  \n**Paper:** General Agent Evaluation — standardize agent interactions across heterogeneous protocols  \n**Pattern:** wshobson marketplace agents/skills/commands as first-class surfaces  \n\n### Module: `src/nexus/agent_protocol.py` (`nexus.agent_protocol/v1`)\n\nUnifying envelope that abstracts tool-calling and CLI:\n\n| Piece | What it does |\n|--------|----------------|\n| `ProtocolTarget` / `ProtocolMessage` | Surface-agnostic invoke/result/error/observe |\n| Surfaces | `tool`, `cli`, `mcp`, `agent`, `skill`, `command` |\n| Normalizers | OpenAI tool_call, Anthropic tool_use, MCP, CLI argv, marketplace, PlanStep, auto-`normalize()` |\n| Converters | `to_openai` / `to_anthropic` / `to_mcp` / `to_cli_argv` / `to_plan_step` |\n| Marketplace | `marketplace_targets()` via existing catalog (shape only, no vendor) |\n| Eval log | `ProtocolTranscript` with summary/validate |\n| Plan bridge | `messages_to_plan` ↔ `plan_to_messages` for multi_llm_agent |\n| CLI | `python -m nexus.agent_protocol normalize\\|validate\\|catalog\\|transcript\\|to-plan` |\n\n### Tests & docs\n\n- `tests/test_agent_protocol.py` — **20 passed**\n- Full suite: **`776 passed, 1 skipped`**\n- Updated: `docs/LATEST_IMPROVE_PLAN.md`, `docs/SELF_IMPROVE_CYCLE.md`, `docs/ALIVE_IMPROVEMENTS.md`\n\n### Files changed (this slice)\n\n| Path | Role |\n|------|------|\n| `src/nexus/agent_protocol.py` | **new** protocol layer |\n| `tests/test_agent_protocol.py` | **new** tests |\n| `docs/LATEST_IMPROVE_PLAN.md` | plan for this cycle |\n| `docs/SELF_IMPROVE_CYCLE.md` | cycle notes |\n| `docs/ALIVE_IMPROVEMENTS.md` | append log |\n\n### Smoke\n\n```text\nnexus.agent_protocol/v1 nexus_status openai\n['tool', 'cli'] ['openai', 'cli']\n```\n\nNo force-push, no secrets, no upstream tree vendored. Orthogonal to existing planners; acts as the dialect bridge only.", 'error': None, 'model': 'grok-4.5', 'exit_code': None}}, {'id': 'novel:arxiv:2601.22129v2+wshobson/agents', 'source': 'cross_pattern', 'selected_as': 'cross_pattern', 'worker': 'grok', 'ok': True, 'result': {'ok': True, 'text': "I'll start by reading the self-improve docs and the plan, then inspect the portfolio idea and relevant code so we can land a small, tested first slice.The user goal targets a different cross-pattern than the landed agent-protocol work — intermediate-state cache/replay hybridized with the marketplace shape. Exploring related code and portfolio evidence next.I'll inspect the orchestrator, marketplace, and similar small modules for patterns we can reuse for intermediate-state cache/replay.Implementing `state_replay`: intermediate-state cache and selective replay (SWE-Replay × marketplace surfaces), plus tests and docs.Fixing marketplace capture to use `PluginInfo.agents/skills/commands`, then adding the orchestrator hook and tests.All unit tests passed. Updating docs and running the full suite.## Landed: SWE-Replay × wshobson marketplace intermediate-state cache\n\nPortfolio item **[cross_pattern] novel:arxiv:2601.22129v2+wshobson/agents** — cache and selectively replay intermediate orchestrator states, with marketplace agents/skills/commands as first-class surfaces.\n\n### What changed\n\n| File | Role |\n|------|------|\n| `src/nexus/state_replay.py` | New module: `StateCache`, capturers, `select_replay` / `ReplayPlan`, CLI |\n| `src/nexus/orchestrator.py` | Soft hook: `meta.state_replay` / `capture_dir` / `capture_marketplace` at task start |\n| `tests/test_state_replay.py` | 15 tests (cache, marketplace, strategies, orch hook, CLI) |\n| `docs/LATEST_IMPROVE_PLAN.md` | This cycle’s plan |\n| `docs/SELF_IMPROVE_CYCLE.md` | Cycle note |\n| `docs/ALIVE_IMPROVEMENTS.md` | Appended log entry |\n\n### Design (shape only, no vendor)\n\n- **SWE-Replay (2601.22129)**: intermediate states keyed by `(task_id, step_id, kind)`; selective replay strategies (`all`, `latest_per_kind`, `latest_per_surface`, `window`); `get_or_capture` for test-time re-use.\n- **wshobson/agents**: marketplace surfaces `agent` / `skill` / `command` + full catalog snapshot via `capture_marketplace`.\n- **Directory capture**: listing metadata only (path/size/mtime) — no file bodies.\n- **Storage**: `.nexus_state/orchestrator/state_cache/<task_id>/states.jsonl` + `index.json`.\n\n### Prove\n\n```bash\nPYTHONPATH=src python3 -m pytest -q tests/test_state_replay.py  # 15 passed\nPYTHONPATH=src python3 -m pytest -q                             # 791 passed, 1 skipped\n```\n\n### Non-goals kept\n\nNo full upstream trees, no file-body blobs, no auto tool re-exec, no force-push/secrets. `engine.replay()` journal timeline remains orthogonal (audit trail, not workspace-state cache).", 'error': None, 'model': 'grok-4.5', 'exit_code': None}}, {'id': 'novel:arxiv:2510.21903v2+wshobson/agents', 'source': 'cross_pattern', 'selected_as': 'cross_pattern', 'worker': 'grok', 'ok': True, 'result': {'ok': True, 'text': 'I\'ll start by reading the self-improve docs and the plan, then implement a small, tested cross-pattern module.The assigned idea is a **User Intent Model** hybrid (arXiv 2510.21903v2 × wshobson marketplace), not the already-landed state-replay work. Exploring the portfolio and existing patterns next.Reviewing orchestrator soft-hooks and a compact recent module for implementation style.Implementing the User Intent Model module and wiring it in.Writing tests and wiring the orchestrator soft hook.All 18 unit tests passed. Updating docs and running the full suite.## Landed: ToM-SWE User Intent Model × wshobson marketplace\n\nImplemented portfolio **[cross_pattern] novel:arxiv:2510.21903v2+wshobson/agents** — a dedicated offline **User Intent Model** that turns ambiguous instructions + interaction history into structured goals/constraints/preferences, with marketplace routing suggestions.\n\n### What changed\n\n| File | Role |\n|------|------|\n| `src/nexus/user_intent.py` | New module `nexus.user_intent/v1` |\n| `src/nexus/orchestrator.py` | Soft hook: `meta.user_intent` → `clarified_goal` + `intent_suggestions` |\n| `tests/test_user_intent.py` | 18 tests |\n| `docs/LATEST_IMPROVE_PLAN.md` | This cycle’s plan |\n| `docs/SELF_IMPROVE_CYCLE.md` | Reasoning map |\n| `docs/ALIVE_IMPROVEMENTS.md` | Cycle log entry |\n\n### Module highlights\n\n- **ToM-SWE shape**: history + instruction → `IntentHypothesis` (goal, constraints, preferences, ambiguity, confidence, clarified instruction)\n- **Persistent memory**: `.nexus_state/orchestrator/user_intent/<user_id>.json` + history JSONL\n- **wshobson surfaces**: ranks agents/skills/commands as `ComponentSuggestion`s\n- **Orchestrator opt-in**: `meta={"user_intent": True, "user_id": "dev"}`\n- **CLI**: `python -m nexus.user_intent infer|observe|memory|history|stats`\n\n### Prove\n\n```bash\nPYTHONPATH=src python3 -m pytest -q tests/test_user_intent.py\n# 18 passed\n\nPYTHONPATH=src python3 -m pytest -q\n# 809 passed, 1 skipped\n```\n\n### Non-goals kept\n\nNo vendored ToM-SWE or wshobson tree, no live LLM, no auto tool exec from suggestions, no force-push.', 'error': None, 'model': 'grok-4.5', 'exit_code': None}}, {'id': 'novel:arxiv:2606.26649v1+IBM/AssetOpsBench', 'source': 'cross_pattern', 'selected_as': 'cross_pattern', 'worker': 'grok', 'ok': True, 'result': {'ok': True, 'text': 'I\'ll start by reading the self-improvement docs and scouting the consensus module so we can land a small Cedar-policy validation slice with tests.Existing Cedar and consensus pieces look relevant — I\'ll inspect them and AssetOpsBench patterns next to shape the hybrid slice.Cedar-in-consensus is already landed; I\'ll hybridize it with AssetOpsBench domain-MCP promote gates and inspect the related modules.I\'ll implement a small hybrid gate: Cedar promote validation for AssetOpsBench domain work plans, with tests and a light consensus hook.Implementing `assetops_work_gate`: Cedar promote validation over AssetOpsBench domain plans, plus tests and docs.Next I\'ll add tests and a soft handoff wire into the AssetOps planner.Related tests are green. Updating the improve-plan docs next, then running the full suite.## Landed: Cedar × AssetOpsBench work-plane promote gate\n\nPortfolio idea **`novel:arxiv:2606.26649v1+IBM/AssetOpsBench`** — fail-closed Cedar validation before promoting multi-domain AssetOps work decisions.\n\n### What changed\n\n| File | Change |\n|------|--------|\n| **`src/nexus/assetops_work_gate.py`** | New hybrid module (`nexus.assetops_work_gate/v1`) |\n| **`src/nexus/consensus.py`** | `promote_decision(..., domain_plan=)` hybrid path |\n| **`src/nexus/assetops_planner.py`** | Opt-in `cedar_gate=` on `plan_and_run` / `plan_and_handoff` |\n| **`tests/test_assetops_work_gate.py`** | Coverage for policies, write safety, thin diagnostic, hooks, CLI |\n| **`docs/LATEST_IMPROVE_PLAN.md`**, **`SELF_IMPROVE_CYCLE.md`**, **`ALIVE_IMPROVEMENTS.md`** | Cycle docs |\n\n### Behavior\n\n1. **Project** an AssetOps plan (+ optional consensus) into a Cedar resource: servers touched, write tools, plan readiness, score/decision.\n2. **Evaluate** domain policies (forbid > permit > deny_default):\n   - unready / empty plan\n   - thin diagnostic (`n_servers < min_domains`)\n   - work-order **write without IoT or FMSR** evidence\n   - healthy multi-domain / write-with-evidence permit\n3. **Soft hooks**:\n   - `promote_decision(verdict, domain_plan=plan)`\n   - `plan_and_run(..., cedar_gate=True)` (default **off** — backward compatible)\n\n### Prove\n\n```bash\nPYTHONPATH=src python3 -m pytest -q tests/test_assetops_work_gate.py\npython -m nexus.assetops_work_gate check-diagnostic "diagnose chiller" --json\n```\n\n### Tests\n\n**824 passed, 1 skipped**', 'error': None, 'model': 'grok-4.5', 'exit_code': None}}, {'id': 'builderz-labs/mission-control', 'source': 'github', 'selected_as': 'fill', 'worker': 'grok', 'ok': True, 'result': {'ok': True, 'text': 'I\'ll explore the mission-control pattern and existing Nexus scaffolding so we can port a small, high-value slice.There\'s already mission-control-shaped scaffolding in the repo. Next I\'ll compare that with the upstream control-plane patterns and pick a small gap to close.I\'ll port mission-control\'s quality-review gate, spend hard-cap, and completion receipts as a small module on top of the existing ops plane.Implementing `mission_gate.py` and tests, then wiring a soft opt-in hook into the control-plane planner.## Landed: mission-control quality gate\n\nPorted operational scaffolding from **builderz-labs/mission-control** (shape only; no tree vendor) onto the existing SQLite ops plane.\n\n### Gap closed\nNEXUS already had jobs + spend (`ops_store`), budget rollups, and a control-plane planner. Missing pieces from mission-control:\n- **Aegis-style quality reviews** before complete  \n- **Spend hard-caps** per job  \n- **Tamper-evident completion receipts**\n\n### First apply slice\n\n| Piece | What |\n|--------|------|\n| `src/nexus/mission_gate.py` | `nexus.mission_gate/v1` on shared `ops.sqlite` |\n| Quality reviews | `approved` / `rejected` / `needs_work` / `pending` |\n| Fail-closed complete | Opt-in via `enable_gate(require_review=True)` |\n| Spend cap | `max_tokens` + `gated_record_spend()` |\n| Receipts | canonicalize → SHA-256 → HMAC-SHA256 (stdlib; same shape as Ed25519 receipts) |\n| CLI | `python -m nexus.mission_gate enable\\|review\\|check\\|complete\\|spend\\|cap\\|verify\\|summary` |\n| Tests | `tests/test_mission_gate.py` (17 cases) |\n\nPlain `OpsStore.set_status` is unchanged — the gate is opt-in through `MissionGate.complete()`.\n\n### Usage sketch\n\n```python\nfrom nexus.mission_gate import MissionGate\n\nwith MissionGate.open(workdir) as gate:\n    gate.enable_gate("job-1", require_review=True, max_tokens=10_000)\n    gate.gated_record_spend("job-1", 120, source="agent:planner")\n    gate.record_review("job-1", reviewer="aegis", status="approved")\n    report = gate.complete("job-1")  # issues receipt\n```\n\n### Files changed\n- **Added:** `src/nexus/mission_gate.py`, `tests/test_mission_gate.py`\n- **Updated:** `docs/LATEST_IMPROVE_PLAN.md`, `docs/SELF_IMPROVE_CYCLE.md`, `docs/ALIVE_IMPROVEMENTS.md`\n\n### Evidence\n`PYTHONPATH=src python3 -m pytest -q` → **841 passed, 1 skipped**', 'error': None, 'model': 'grok-4.5', 'exit_code': None}}], 'decision': {'ok': True, 'reason': 'apply_allowed', 'confidence': 1.0, 'candidate': {'repo': 'wshobson/agents', 'score': 16.0, 'idea': 8.0, 'skill': 8.0, 'method': 'grok:grok-4.5', 'path': '.nexus_workspaces/mine_eval/wshobson__agents', 'pattern': 'Markdown source-of-truth for agents/skills/commands + harness generators', 'rank': 21.0, 'evidence_hits': 5, 'on_spine': True, 'spine_method': 'grok:grok-4.5', 'spine_run_id': 'alive-wshobson_agents', 'spine_score': 16.0, 'spine_boost': 1.0, 'spine_grade_id': '4c85f45ea3984748'}}, 'signal': 'continue', 'alias': 'self_approve_apply', 'phase': 'implement', 'note': 'min 1 arxiv + 1 github, max 10 ideas'}

## Artifacts

- `docs/LATEST_GITHUB_REVIEW.md`
- `docs/LATEST_ARXIV_IMPROVE.md` / `.nexus_state/arxiv_improve/PAPER_IMPROVE.md`
- `docs/LATEST_DUAL_REVIEW.md`
- `docs/LATEST_IMPROVE_PLAN.md`

## Residual / next

- Landed apply; re-run official harness if SWE-Pro is the goal.

## Apply payload (truncated)

```
{"ok": true, "implemented": 10, "total": 10, "results": [{"id": "arxiv:2606.26649v1", "source": "arxiv", "selected_as": "required_arxiv", "worker": "grok", "ok": true, "result": {"ok": true, "text": "I'll implement a Cedar Policy Language validation step in the consensus path before promotion. First I'll read the self-improve docs and locate the consensus module.Next I'll inspect the consensus module and any existing Cedar/policy-as-code patterns to wire validation before promotion.I'll inspect how consensus feeds promotion and which security-gate patterns already exist so we can add a Cedar-style validation step cleanly.Implementing a Cedar-shaped policy validator and wiring it into consensus before promote.Fixing a couple of parser edge cases, then wiring the promote gate into consensus and the engine.Checking promote tests so the Cedar gate doesn't break the single-judge path.Adjusting the engine promote path so single-judge verdicts aren't blocked by the multi-grader Cedar defaults.The preview truncates before any `permit` lines \u2014 adjusting the test and the preview content.All targeted tests passed. Updating docs and running the full suite.## Landed: Cedar policy-as-code before consensus promote\n\n**Source:** [arXiv 2606.26649v1](https://arxiv.org/abs/2606.26649v1) \u2014 *Autoformalization of Agent Instructions into Policy-as-Code*  \n**Concrete change:** Cedar-shaped validation in the consensus module before promoting a decision.\n\n### What changed\n\n| File | Role |\n|------|------|\n| `src/nexus/cedar_policy.py` | New offline Cedar subset (`nexus.cedar_policy/v1`): permit/forbid, forbid > permit > deny_default, when-atoms, text parse/dump |\n| `src/nexus/consensus.py` | `validate_promote`, `promote_decision`, `ConsensusVerdict.apply_promote_gate`; every aggregate attaches `promote_allowed` + `cedar_policy` |\n| `src/nexus/engine.py` | Review\u2192promote path runs Cedar after IndependentVerify (opt-out: `meta.cedar_policy=false`) |\n| `tests/test_cedar_policy.py` | Policy eval, parse, promote gate, consensus integration |\n| `tests/test_consensus.py` | Aggregate + engine promote Cedar coverage |\n| `docs/LATEST_IMPROVE_PLAN.md`, `docs/SELF_IMPROVE_CYCLE.md`, `docs/ALIVE_IMPROVEMENTS.md` | Cycle docs |\n\n### Behavior\n\nDefault promote policies (fail-closed):\n\n- **Forbid:** `fail` / veto-family, `degraded`, low score, low agreement  \n- **Permit:** healthy `pass` (and high-score `revise`) with enough graders  \n- Engine still uses Independ
```
