# NEXUS demo reel (privacy-safe)

Generated from **live** bus agent status (`http://127.0.0.1:3099/api/status`) plus public cycle metrics from the last REAL summary.

## Files

| File | Use |
|------|-----|
| `nexus-demo-reel.mp4` | **X / LinkedIn / GitHub** (~29s, 1280×720) |
| `nexus-demo-reel.gif` | README embed preview |
| `01-…08-*.png` | Still frames / thumbnails |

Also copied to repo root assets:

- `docs/assets/nexus-demo-reel.mp4`
- `docs/assets/nexus-demo-reel.gif`

## X post (copy-paste)

```
Built a multi-LLM product loop that finishes work:

• Shared bus — Claude / GPT / Grok / Gemini / local all online
• Grok implements → panel critiques → synthesis
• REAL cycle: 10/10 portfolio ideas, tests green

Clone & run live:
git clone https://github.com/VincentMarquez/nexus-core.git
cd nexus-core && make install && make start && make demo-all-quick

github.com/VincentMarquez/nexus-core
```

Upload **`nexus-demo-reel.mp4`** as native video on X (not a link to a file host).

## Privacy

- No home directories, API keys, or git identity in frames
- Agent ONLINE flags came from live `/api/status` at generation time
- Full-desktop screenshots intentionally excluded (too easy to leak personal UI)

## GitHub README embed

```markdown
<p align="center">
  <img src="docs/assets/nexus-demo-reel.gif" alt="NEXUS multi-LLM demo reel" width="800">
</p>
```
