# S04 offline review — Grok

**ts:** 2026-07-17  
**Prompt:** same as `reviews/S04-OFFLINE-PACKET.md`  
**Inputs:** PRINCIPLES.md, steps/S04-idea-scope-contract.md, references/BUBBLES_DNA_AND_SARSI.md

---

## 1. Verdict

**approve-with-nits**

Safe to implement as **v1 soft-only** (generate contract + inject short DNA + write `CONTRACT.json` + optional soft path filter). Do **not** enable hard enforce or publish intersection in this step.

---

## 2. Risks

| Severity | Risk |
|----------|------|
| **high** | None if hard enforce stays off and inject is pure prepend. |
| **med** | Default `allowed_prefixes` too narrow → Grok “violates” contract while doing legitimate work (e.g. `Makefile`, `plugins/`, `skillpacks/`, root `pyproject.toml`) → noise or future hard-gate false fails. |
| **med** | DNA block too long → eats attention; Grok ignores scope (D*=0 fails). Cap ≤40 lines. |
| **med** | Soft `paths_in_scope` on panel slice could *shrink* review set incorrectly if prefixes wrong — prefer log-only in v1, don’t drop files from panel unless flag. |
| **low** | Double-encoding / goal string growth per idea (10 ideas × DNA) — keep DNA short. |
| **low** | Sparse ideas (missing title/concrete) → weak mission text; must still produce valid schema. |
| **low** | `success_check.paths` pointing at non-existent tests → don’t fail implement on that in S04. |

---

## 3. Missing tests

Must have in `tests/test_scope_contract.py`:

1. `default_contract` always has required keys: `schema`, `idea_id`, `mission`, `allowed_prefixes`, `forbidden_prefixes`, `non_goals`, `success_check`, `max_files`.  
2. Sparse idea `{}` / only `id` still yields valid contract (fail-open defaults).  
3. `paths_in_scope`: in-prefix kept; forbidden rejected; empty path list ok.  
4. `format_dna_block`: non-empty, includes `idea_id`, length budget (e.g. &lt; 2500 chars).  
5. `validate_contract`: returns warnings list, does not raise on soft mode.  
6. Integration-ish: mock idea → DNA prepend contains `<NEXUS_SCOPE_CONTRACT>` or agreed tag (unit-level string check).  
7. `write_contract` creates `CONTRACT.json` under tmp pack dir.

Nice:

8. Source-specific prefixes (github adds `plugins/` if specified).  
9. `scope_contract_enable=false` path leaves goal unchanged (if tested at portfolio level with monkeypatch).

---

## 4. Simpler alternative

**Minimal viable S04:**

- Only `default_contract` + `format_dna_block` + write `CONTRACT.json` in review pack.  
- **No** panel path filtering in v1.  
- **No** publish intersection (S02 already cycle-scopes).  
- Soft enforce = log warning helper only, not called from hot path until stable.

That still ports Bubbles/DNA discipline (mandatory fields + preamble) with less blast radius.

---

## 5. Checklist items to add to the step file

- [ ] Schema constant `nexus.scope_contract/v1` frozen  
- [ ] DNA block ≤40 lines / ≤2.5k chars  
- [ ] Tag name fixed: e.g. `<NEXUS_SCOPE_CONTRACT>`  
- [ ] v1: soft inject only; panel filter **log-only or off**  
- [ ] v1: no publish ∩ contract  
- [ ] `success_check.type` allows `pytest_paths` | `py_compile` | `manual` | `none`  
- [ ] Flags on AliveConfig + alive.json round-trip (or env only if smaller)  
- [ ] Exception in contract build never aborts implement (try/except → no DNA)  
- [ ] Document default prefixes include at least `src/nexus/`, `tests/`, `docs/` and note Makefile later  
- [ ] Export from `nexus` package if needed for tests  

---

## 6. Bubbles/DNA analogy correct?

**Yes.** Correctly framed as *discipline* (mandatory genome / D*=0 preamble), not as rebuilding UniversalBubble or the event bus. SARSI mapping is inspirational, not over-claimed as shipped code.

---

## 7. Live REAL self-improve loop breakage?

| Risk | Mitigation |
|------|------------|
| Import error in new module | try/except around inject; tests import smoke |
| Prompt too large | short DNA |
| Panel breaks if MANIFEST shape changes | additive fields only |
| Config keys unknown | getattr defaults |
| Hard enforce on by mistake | default `false`; no hard path in v1 code at all |

**Does not break** S01/S02/S03 if contract is additive.

---

## A / B / C

### (A) must-fix before merge

1. New `scope_contract.py` with pure functions.  
2. Build + prepend DNA in `implement_portfolio` (try/except).  
3. Write `CONTRACT.json` from `write_review_pack` or implement path.  
4. Unit tests listed above.  
5. No hard enforce; no publish change.  

### (B) nice-to-have

1. AliveConfig flags for enable/soft.  
2. Slightly richer default prefixes (`plugins/`, `skillpacks/`, `Makefile` as exact allow).  
3. Log out-of-scope paths after implement if soft_enforce.  

### (C) out of scope

- SARSI signing, foundry, self-model  
- Bubbles SystemContext rewrite  
- Accept predicate (S05)  
- Quarantine promote (S06)  
- Hard path jail abort  

---

## Recommended implement shape (for when both reviews agree)

```text
1. src/nexus/scope_contract.py
2. idea_portfolio.implement_portfolio: contract = default_contract(idea); goal = dna + goal
3. critique_panel.write_review_pack: write CONTRACT.json if contract passed in
4. tests/test_scope_contract.py
5. optional AliveConfig.scope_contract_enable = True
```

Proceed to code only after GPT review lands and no **request-changes** on (A).
